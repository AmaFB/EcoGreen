<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:name=".EcoGreen"
    package="com.example.ecogreen">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EcoGreen">

        <activity android:name=".Achievement" />
        <activity android:name=".Account_set" />
        <activity android:name=".Sustainable" />
        <activity android:name=".FindMarket" />
        <activity android:name=".Activities" />
        <activity android:name=".Personal" />
        <activity android:name=".Reg" />
        <activity android:name=".Login" />
        <activity android:name=".Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>